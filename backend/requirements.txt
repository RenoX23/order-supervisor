# Web / API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# Orchestration
temporalio>=1.9,<2.0

# Persistence
asyncpg>=0.30,<0.31

# Config / models
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# LLM (agent step) — OpenAI-compatible client (Groq by default)
openai>=1.50,<2.0

# Dev / tests
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
